# chat
Sample chat app using node and socket.io

- Run the server: node server.js
- Open in a web browser: http://localhost:3000
- Open the same url in a new tab, using incognito mode.
- Now you can send messages to all the users connected on this server.
